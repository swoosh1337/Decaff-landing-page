/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'existing-domain.com',
      'hebbkx1anhila5yf.public.blob.vercel-storage.com'
    ]
  }
}

module.exports = nextConfig 